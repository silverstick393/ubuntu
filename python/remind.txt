a = ['a','b','c']
b = []
print("연산전 a의 id : ",id(a))
print("연산전의 b의 id : ", id(b))
print("연산전 b의 값",b)
# b = a --> b와 a는 값도 주솟값도 같다. 
# b = a[:] --> a의 사본을 만들어서 b에 대입을 하기 떄문에, b의 주소와 값은 모두 달라지지만 a와는 별개로 움직인다.
# b[:] = a --> a의 값을 그대로 복사하여 b에 대입. 하지만 둘의 주소값은 다르다.
# b[:] = a[:] 
print("연산 후 a의 id : ",id(a))
print("연산 후의 b의 id : ", id(b))
print("연산후 b의 값",b)